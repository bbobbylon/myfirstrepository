<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!--
    RefillRadar - personal early warning for drug shortages.

    Parent POM inheritance: spring-boot-starter-parent pins consistent, mutually
    compatible versions for every Spring and third-party library we use. This is why
    the <dependency> blocks below carry no <version> tags - the parent decides, so we
    cannot accidentally pair an old Jackson with a new Spring and spend an evening on a
    NoSuchMethodError.
  -->
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.16</version>
    <relativePath/>
  </parent>

  <groupId>com.refillradar</groupId>
  <artifactId>refillradar</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <name>RefillRadar</name>
  <description>Warns patients about drug shortages before their refill fails</description>

  <properties>
    <java.version>21</java.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencies>
    <!-- REST endpoints + embedded Tomcat. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>

    <!-- Bean Validation (@NotNull, @Positive) on incoming API payloads. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>

    <!-- SMTP delivery for v0.2 alerts. Only active when refillradar.alert-channel=email,
         so a deployment without mail configured still starts and logs alerts instead. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-mail</artifactId>
    </dependency>

    <!-- JUnit 5, AssertJ, Mockito, MockMvc. Test scope only - never shipped. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- Produces the executable fat JAR: java -jar target/refillradar-0.1.0-SNAPSHOT.jar -->
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
